# Java

✅ Java Components: JDK, JRE, JVM (in sequence)
🔷 1. JDK (Java Development Kit)
What it is: The full software kit to develop, compile, and run Java programs.

Includes: JRE + Compiler + Debugger + Tools.

Use: For developers.

📌 Without JDK, you can’t write or compile Java code.

🔷 2. JRE (Java Runtime Environment)
What it is: Environment to run Java programs.

Includes: JVM + Libraries.

Use: For users who just want to run Java apps.

📌 JRE can run Java code, but not compile it.

🔷 3. JVM (Java Virtual Machine)
What it is: The engine that executes Java bytecode.

Platform Independent.

Use: Actually runs the program.

📌 Part of both JDK and JRE.

📌 Sequence:
JDK → JRE → JVM

JDK (developer tools) ⟶ includes → JRE (runtime) ⟶ includes → JVM (executes code)

✅ Git (Version Control Tool)
🔷 Git
What it is: A tool to track changes in code, manage versions, and collaborate.

Use: Developers use Git to keep history of code, create branches, and collaborate using platforms like GitHub.

Key Features: Commit, Push, Pull, Merge, Branch, Clone, etc.

📌 Summary Table:
Term	Full Form	Purpose	Used By
JDK	Java Development Kit	To develop and compile Java code	Developers
JRE	Java Runtime Environment	To run Java apps	Users & Developers
JVM	Java Virtual Machine	Executes bytecode	Inside JRE
Git	—	Version control tool for managing code	Developers
